# Development dependencies.
# Kept in step with [dependency-groups].dev in pyproject.toml:
# CI installs this file, `uv sync` installs the group, and a test
# fails when they disagree.
# black is deliberately absent: ruff format is the formatter, and
# two formatters with different opinions fight over the same files.
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
ruff>=0.11.0
mypy>=1.15.0
pre-commit>=3.0.0
detect-secrets>=1.5.0
bandit>=1.7.0
radon>=6.0.0
diff-cover>=9.0.0
jsonschema>=4.20.0
