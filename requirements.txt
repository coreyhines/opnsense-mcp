# Runtime dependencies. The image installs from this file, so it must
# match [project].dependencies in pyproject.toml; a test fails when they
# drift. Floors are the versions actually tested, not historical minimums.
fastapi>=0.141.1
uvicorn>=0.52.4
ruamel.yaml>=0.19.1
pydantic>=2.13.5
requests>=2.34.2
python-multipart>=0.0.32
typing-extensions>=4.16.0
passlib[bcrypt]>=1.7.4
python-dotenv>=1.2.3
fastmcp>=4.0.0b5,<5
pyyaml>=6.0.3
paramiko>=5.0.0
idna>=3.19
starlette>=1.6.0
pydantic-settings>=2.15.0
